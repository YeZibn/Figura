## 1. Conda Environment And Result Contract

- [x] 1.1 Verify the Conda `agent` environment and install the project with dev dependencies using `conda run -n agent python -m pip install -e '.[dev]'`; confirm `rapidocr`, Pillow, pytest, and the package import from that interpreter.
- [x] 1.2 Add tests for generated-image and enriched tool-result value objects, including valid data, captions, warnings, immutable byte payloads, supported MIME types, size limits, and image-count limits, executed with `conda run -n agent pytest ...`.
- [x] 1.3 Implement the enriched result value objects and normalization path while keeping plain JSON-serializable tool return values valid.
- [x] 1.4 Add and implement enriched dispatch so Agent callers receive JSON content plus validated images, while the existing `dispatch()` function preserves its current JSON-string contract for legacy callers and MCP conversion.
- [x] 1.5 Cover malformed data, tool exceptions, empty/unsupported/oversized images, excess images, and arbitrary path-like image declarations as structured errors or non-fatal warnings without losing valid sibling results.

## 2. Multimodal Observation Transport

- [x] 2.1 Extend multimodal content helpers to build captioned `image_url` parts from validated generated bytes without reading a tool-declared local path.
- [x] 2.2 Update the Agent loop to append every native JSON `tool` message first and then one combined multimodal evidence turn for all valid images from that assistant tool-call batch.
- [x] 2.3 Include a fixed evidence preamble plus tool name, `tool_call_id`, and caption for every generated image, and test exact association and ordering for one and multiple tool calls.
- [x] 2.4 Preserve byte-for-byte history behavior for JSON-only calls and verify that invalid images do not add a multimodal turn or stop the loop.
- [x] 2.5 Release retained generated payloads on Agent reset and close while leaving user-provided source images untouched, with lifecycle tests.
- [x] 2.6 Update the default Agent guidance to describe visual evidence and optional retry/tool-switch behavior without requiring a fixed sequence, threshold, validation action, or ChartSpec response.

## 3. Chart Tool Overlays

- [x] 3.1 Add deterministic OCR overlay rendering that preserves source dimensions, assigns stable 1-based IDs to structured OCR entries, and visibly marks each text box and confidence.
- [x] 3.2 Return the OCR data and PNG overlay as one enriched result, covering successful detection, no-text detection, and missing/unreadable source images.
- [x] 3.3 Add deterministic bar overlay rendering that preserves source dimensions, assigns stable 1-based IDs, and visibly marks every returned bar, top edge, and detected baseline.
- [x] 3.4 Return bar measurements and the PNG overlay as one enriched result, covering successful detection, no-bar detection, and missing/unreadable source images.
- [x] 3.5 Add image-level assertions that overlays differ from the source where detections exist and that structured IDs correspond to visible overlay labels.

## 4. Agent Recovery And Regression Coverage

- [x] 4.1 Add an offline recording-client test proving the model receives the original chart, JSON tool output, and generated overlay on the next turn.
- [x] 4.2 Add a scripted recovery trajectory in which a first visual observation is rejected, the model requests a second tool call with adjusted arguments, and the second observation leads to a final answer.
- [x] 4.3 Update the chart-understanding end-to-end fixture to exercise enriched OCR and bar results without imposing a production tool order.
- [x] 4.4 Run focused tool, multimodal, Agent, CLI, MCP-conversion, and chart-understanding tests through `conda run -n agent pytest ...` and resolve regressions.

## 5. Provider Compatibility And Final Verification

- [x] 5.1 Add a live smoke that uses the configured multimodal model to execute a chart tool, receive its overlay after all native `tool` messages, and accurately describe the marked evidence without a caller-imposed tool sequence.
- [x] 5.2 Run the live smoke with `conda run -n agent python ...` against the configured OpenAI-compatible endpoint and record the accepted message sequence, model, and bounded diagnostic output without exposing credentials.
- [x] 5.3 Run the complete suite with `conda run -n agent pytest -q` and validate the OpenSpec change with `openspec validate add-multimodal-tool-observations --strict`.
