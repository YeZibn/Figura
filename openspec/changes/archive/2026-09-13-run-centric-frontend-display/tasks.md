## 1. Run 级会话展示

- [x] 1.1 在会话面板中按创建时间构建 Run 展示块，固定渲染用户请求、可展开执行过程和最终结果三个阶段。
- [x] 1.2 让真实 Gateway 返回的规范 `${runId}` 贯穿消息关联与实时提交，在拿到 Run ID 后确保提问和回答进入同一个 Run。
- [x] 1.3 为无法关联 Run 的历史消息保留兼容展示，验证旧消息不会因 Run 分组而消失或重复。

## 2. 执行过程与结果产物

- [x] 2.1 保持工具调用、工具结果和视觉观察的现有关联与展开能力，在执行轨迹中保留有界的图表生成事件提示。
- [x] 2.2 从 Run 事件中提取生成图表产物，并将可用或已失效状态渲染到最终结果区域，避免同一图表重复显示。
- [x] 2.3 确保文本回答继续使用安全 Markdown 渲染，并在仅有图表产物时仍显示完整的最终结果区域。

## 3. Mock 数据与界面样式

- [x] 3.1 更新 Mock 客户端和演示数据，使新建 Run 的用户消息、助手消息和 Run ID 遵循同一稳定关联约定。
- [x] 3.2 增加 Run 容器、最终结果和图表产物区域的样式，保持执行详情、消息折叠和窄窗口布局可用。

## 4. 验证与回归

- [x] 4.1 使用现有前端检查执行 `npm run build`、`npm run smoke` 和 `git diff --check`，修复构建、冒烟或格式问题。
- [x] 4.2 验证 Mock 模式下已完成 Run、运行中的 Run、仅图表结果、图表失效和旧消息兼容展示。
- [x] 4.3 验证 Gateway 模式刷新或重新进入会话后 Run 的用户请求、执行过程、最终回答和图表产物仍保持同一结构，且不出现重复孤立消息。

## 5. Gateway 与 Agent Run 身份统一

- [x] 5.1 让持久化 Agent Memory 支持 Gateway 提供的外部 Run ID，同时保留独立 CLI 调用自动生成 Run ID 的兼容行为，并拒绝跨会话 ID 冲突。
- [x] 5.2 将 Gateway 创建的 Run ID 传入 Agent Runtime、Agent Memory 和 trace emitter，确保运行记录、执行事件、最终回答和附件绑定使用同一身份。
- [x] 5.3 校验会话投影使用规范 Run ID 生成用户/助手消息 ID，并为旧的 split-identity 数据提供明确的兼容状态而不静默错误合并。

## 6. 集成回归验证

- [x] 6.1 增加 Gateway 集成测试，断言一次真实运行返回的 Run summary、消息 ID、执行事件和生成产物使用同一个 Run ID。
- [x] 6.2 增加多次运行和历史恢复测试，断言每个 Run 的提问、执行过程和最终结果只出现一次且顺序稳定。
- [x] 6.3 使用 `conda run -n agent` 执行相关 Python 测试，并执行前端 `npm run build`、`npm run smoke` 和 `git diff --check`。
