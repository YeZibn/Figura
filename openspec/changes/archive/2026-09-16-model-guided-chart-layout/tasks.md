## 1. 布局上下文与校验基础设施

- [x] 1.1 定义可序列化的 `ChartLayoutContext` 数据契约，统一源图尺寸、坐标模型、方向、`measurement_frame`、`annotation_regions`、置信度、证据和校验状态字段
- [x] 1.2 实现归一化区域到源图像像素坐标的转换，以及边界、最小尺寸、区域角色和轴关系校验
- [x] 1.3 实现颜色轨迹覆盖、明显图像边界、模型置信度和冲突证据校验，区分 accepted、partial 和 rejected，并保留可检查的像素证据
- [x] 1.4 将布局上下文和校验结果接入共享 chart observation foundation，保持没有上下文时的传感器兼容路径

## 2. Agent 布局预检链路

- [x] 2.1 增加 provider-neutral 的图表布局预检入口和结构化结果约束，使模型只能提交归一化布局提示而不能提交本地路径或图像字节
- [x] 2.2 在授权图片加载之后、专用图表传感器之前按 run 执行一次布局预检，并缓存与附件/run 绑定的已校验上下文
- [x] 2.3 将已接受或部分接受的上下文安全地传递给柱状图、折线图、散点图和饼图工具；预检失败时回退到确定性传感器路径并报告状态
- [x] 2.4 更新 Agent 工具说明和上下文策略，明确布局提示是 advisory evidence，禁止据此直接编造坐标轴、日期或数据值

## 3. 四类图表传感器接入共享布局

- [x] 3.1 更新 Cartesian frame/axis 检测，在上下文存在时限制测量范围，并对浅色坐标轴、暗色文字伪轴、轴连续性、正交关系和残差进行独立验证
- [x] 3.2 让柱状图和散点图消费统一的测量框及标注区域，保持各自柱子/基线和 marker 的独立测量逻辑、方向支持与不确定性输出
- [x] 3.3 让饼图消费同一布局上下文的 polar 区域表达，保持中心/半径与 Cartesian 轴字段隔离，并保留图例和外部标签关联
- [x] 3.4 统一四类传感器的 evidence envelope、字段命名、来源标记、置信度和 warning 语义，删除或停止产出已废弃且无消费方的重复字段

## 4. 折线图测量改造

- [x] 4.1 在 `extract_line_series` 中按已校验测量框建立 trace mask，排除图例色块、旋转刻度、标题和数据标注对轴/轨迹/点候选的污染
- [x] 4.2 将 X 轴数字、日期和分类文本统一为有序 anchor evidence，记录文本及源像素位置；只有存在可靠语义映射时才生成 numeric X 值
- [x] 4.3 显式区分 marker 点和 `tick_sample` 点，移除无锚点的像素密度峰值伪点和连续折线伪点，并在无可靠采样锚点时只返回连续轨迹及 warning
- [x] 4.4 完善小角度旋转/横向布局的坐标投影、系列分离、交叉处理、校准残差和置信度降级，禁止用固定裁剪边界替代坐标证据
- [x] 4.5 更新折线 overlay，使其保持源图尺寸并标记测量框、轴、轨迹、确认点、系列身份、校准状态和冲突区域

## 5. 执行事件与前端时间线

- [x] 5.1 修改 Gateway `RunEvent` 的截断策略，将 `tool_name`、`call_id`、status、turn 和 run correlation 保留在外层，仅截断/摘要诊断 result body
- [x] 5.2 为旧格式、持久化恢复、SSE 和超长 trace/OCR 结果增加兼容解析，确保事件大小仍有上限且不泄露本地路径或敏感内容
- [x] 5.3 更新 React 时间线归并逻辑，按稳定调用标识保留截断结果所属工具步骤，并显示明确的 bounded/truncated 状态而不是“未知工具”
- [x] 5.4 覆盖缺失调用标识或历史断裂场景，继续显示显式 incomplete/unavailable 状态且不虚构工具步骤
- [x] 5.5 将 `inspect_chart_layout` 纳入 `CHART_TOOLS` 和统一授权附件适配，移除 Gateway 的重复单独注册路径
- [x] 5.6 为布局预检增加中文工具展示映射，收紧模型描述的统一字段和校验状态语义，并覆盖前端 trace label 的回归测试

## 6. 回归验证与真实链路验收

- [x] 6.1 为布局上下文序列化、归一化坐标、校验状态、冲突降级和 sensor-only fallback 添加单元测试
- [x] 6.2 为真实双序列折线图增加回归 fixture，验证浅色 Y 轴不再被漏检、不会生成伪斜轴、旋转日期标签不进入 frame，且无 marker 时不产生伪点
- [x] 6.3 增加正向、横向、小角度旋转、数字/日期/分类 X 轴、交叉系列、无标定和不完整证据的柱状图、折线图、散点图和饼图测试
- [x] 6.4 为 Agent 预检复用、Gateway 超长事件持久化/SSE、前端调用归并和截断提示增加回归测试
- [x] 6.5 使用 `conda run -n agent python -m pytest -q`、`npm run build` 和 `npm run smoke` 完成验证，并通过前端 Gateway 链路重新运行真实附件，记录布局校验状态、折线采样来源和 warning
