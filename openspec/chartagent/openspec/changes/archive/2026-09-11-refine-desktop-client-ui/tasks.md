## 1. Figura 品牌与兼容边界

- [x] 1.1 将 `frontend/src/App.tsx`、mock 展示数据和前端错误/空状态中的用户可见 `ChartAgent` 文案统一为 `Figura` 或 `Figura Agent`，保留必要的工具名和技术标识。
- [x] 1.2 更新 `src-tauri/tauri.conf.json` 的产品名和窗口标题为 Figura，同时保留现有应用 `identifier`、Python 模块路径、环境变量前缀和数据目录。
- [x] 1.3 将 Gateway 面向人的服务展示名称更新为 `Figura Gateway`，同步更新项目内相关测试断言，不改变版本化协议结构、路由、请求头或错误码。
- [x] 1.4 更新 README、启动说明和相关用户-facing 文档中的产品名称，明确 Figura 是产品名、`chartagent` 是当前内部兼容标识。

## 2. React 工作区结构

- [x] 2.1 在不改变 `ChartAgentClient`、SSE 事件到 `ConversationItem` 转换和附件预览生命周期的前提下，整理 `App.tsx` 的区域组件和 JSX 结构。
- [x] 2.2 重构会话导航区域，强化 Figura 品牌、当前会话选中态、更新时间、运行次数和服务状态，并为未来会话级操作保留独立且不嵌套按钮的操作槽位；本任务不增加删除按钮或删除调用。
- [x] 2.3 优化对话区域的用户消息、Figura Agent 回复、工具调用、工具结果和视觉观察的层级，确保执行详情仍可展开且不会压过主要答案。
- [x] 2.4 优化输入区、附件区、加载态、空会话态、无会话态、Gateway 不可用态和错误反馈，确保所有新增用户可见文案使用简体中文并采用 Figura 品牌。

## 3. 视觉系统与响应式布局

- [x] 3.1 将 `frontend/src/styles/global.css` 整理为可维护的视觉令牌和分区样式，统一背景、正文、边框、品牌、运行中、成功、错误、间距、控件高度、阴影和焦点表现。
- [x] 3.2 调整桌面三栏布局和中等宽度布局，保证主对话、输入区和附件检查区具有明确优先级，避免固定宽度、长文案或状态切换造成溢出和布局跳动。
- [x] 3.3 调整窄窗口布局为可用的会话导航、主对话、输入区和附件区域，确保关键操作不依赖 hover，文本和图标不会互相遮挡。
- [x] 3.4 为会话、附件、执行详情、对话框和发送控件补充一致的 hover、focus、disabled 和错误状态，并校验图标按钮具有中文可访问名称或 tooltip。

## 4. 文档、测试与验证

- [x] 4.1 更新前端 smoke 检查和 Python/Tauri 相关名称断言，覆盖 Figura 展示名以及 `chartagent` 内部兼容入口仍然存在。
- [x] 4.2 在前端目录执行 `npm run build` 和 `npm run smoke`，确认 mock 与 Gateway 客户端边界、现有实时事件和附件能力没有回归。
- [x] 4.3 使用 `conda run -n agent python -m pytest` 执行 Python Gateway、运行时和客户端测试，确认健康响应、启动链路和配置兼容性不受品牌改名影响。
- [x] 4.4 在浏览器开发模式检查宽、中、窄窗口，确认 Figura 文案、状态层级、键盘焦点、空状态和附件预览表现一致；Tauri 实机验证延期到后续独立工作。

> 4.4 验证记录：浏览器开发模式当前页面已确认 Figura、Gateway、Agent、对话输入、附件和执行详情正常；窄窗口纵向布局已检查。宽、中窗口通过 1120px、760px、470px 响应式断点、布局结构和前端 build/smoke 验证。Tauri 实机验证不属于当前变更范围。
