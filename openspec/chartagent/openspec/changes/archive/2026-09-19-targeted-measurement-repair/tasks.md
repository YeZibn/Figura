## 1. 测量修复契约与数据模型

- [x] 1.1 定义有界的 `MeasurementTarget`、修复动作、父 attempt 关联、目标指纹和修复预算数据结构
- [x] 1.2 扩展质量问题、测量结果和装配门禁的序列化契约，保留无目标旧调用和旧 checkpoint 的兼容行为
- [x] 1.3 实现来源图片、panel、父 attempt、工具类型、目标区域和受影响字段的统一校验
- [x] 1.4 实现重复目标识别和修复预算计算，确保相同目标不会产生无界重复 attempt
- [x] 1.5 为目标边界、跨 panel 拒绝、旧格式兼容、重复目标和预算耗尽补充单元测试

## 2. 四类测量工具的局部观测

- [x] 2.1 实现共享的 focus target 解析、panel 相交判断、源图坐标裁剪和局部到源图坐标映射
- [x] 2.2 为柱状图测量接入可选目标区域，覆盖基准线、柱体、刻度和值标签等受影响字段
- [x] 2.3 为折线图和散点图测量接入可选目标区域，保留轴、折线/点、数据映射和局部几何关系
- [x] 2.4 为饼图测量接入可选目标区域，保留中心、扇区、标签、图例和占比关系
- [x] 2.5 保证局部测量仍返回完整证据、质量状态、覆盖范围、source attribution 和 overlay 坐标
- [x] 2.6 为四类工具补充正常目标、部分目标、无效目标、跨 panel 目标和目标外数据的回归测试

## 3. Agent 内置修复循环

- [x] 3.1 将测量门禁输出映射为结构化修复上下文，包含目标区域、受影响字段、父 attempt、原因和下一步动作
- [x] 3.2 在 Agent 下一轮上下文中注入修复目标，并约束模型继续使用同一 run、attachment、panel 和 measurement session
- [x] 3.3 实现修复 attempt 的父子 lineage、目标指纹去重和默认最大修复次数
- [x] 3.4 将新 attempt 送回同一质量审计和装配门禁，禁止把未通过审计的局部结果直接用于最终装配
- [x] 3.5 处理修复成功、修复后仍不完整、来源不一致、重复目标和预算耗尽等终止状态
- [x] 3.6 保证断点恢复、重连和重复请求不会重复创建等价修复 attempt
- [x] 3.7 验证直接视觉装配在没有测量 session 时保持原有路径，不被错误带入修复循环

## 4. Gateway、checkpoint 与执行追踪

- [x] 4.1 增加测量修复请求、完成、拒绝和预算耗尽的结构化 trace 事件
- [x] 4.2 对 trace 和 API 返回中的目标、attempt、错误和下一步动作做有界化与敏感信息清洗
- [x] 4.3 保持 run、attachment、panel、turn 和 tool call 的外层身份字段，补充父子 attempt 关联
- [x] 4.4 为 checkpoint 保存和恢复补充 pending repair、accepted attempt、预算及 lineage 状态
- [x] 4.5 为重连后的事件重放、幂等请求和修复终止状态补充 Gateway/会话回归测试

## 5. 真实图表链路评估

- [x] 5.1 建立不包含密钥和绝对路径的评估清单，覆盖 dashboard、旋转/横向/分组/堆叠柱状图、多序列折线图、饼图和散点图
- [x] 5.2 为每类样本记录 panel 识别、测量字段覆盖、来源坐标映射、质量状态和最终装配状态等基线指标
- [x] 5.3 验证“初次测量失败 → 指定局部区域重测 → 重新审核 → 接受装配”的四类图表端到端链路
- [x] 5.4 验证局部区域不足、旋转/横向几何、重复目标、跨 panel、checkpoint 恢复和预算耗尽等失败路径
- [x] 5.5 对比改造前后误接受、误拒绝、无效重测、重复重测和最终可装配率，并记录可接受阈值

## 6. 完成前验证

- [x] 6.1 先运行测量模型、四类工具、Agent 修复和 Gateway 回归测试，再运行完整 Python 测试套件
- [x] 6.2 使用 `conda run -n agent` 验证 canonical runtime、rapidocr 导入和完整 pytest 结果
- [x] 6.3 运行 `git diff --check`、`openspec validate --all --strict` 和必要的类型/编译检查
- [x] 6.4 记录兼容性、默认修复预算、checkpoint 迁移行为和无新增运行时依赖的结论
