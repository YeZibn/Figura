schema: spec-driven
store: chartagent

context: |
  This repository has separate OpenSpec stores for the existing chartagent implementation and the new Figura implementation.
  Use the chartagent store for legacy implementation work. For work on the new implementation under src/figura, select the figura store explicitly with --store figura.
